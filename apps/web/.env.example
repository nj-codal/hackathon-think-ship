# Required
NEXT_PUBLIC_SANITY_PROJECT_ID=PROJECTID
NEXT_PUBLIC_SANITY_DATASET=DATASETNAME

# Optional — needed for Live Content API / draft mode
SANITY_API_READ_TOKEN=

# Google Maps — needed for the map view and Sanity Studio geopoint input
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
SANITY_STUDIO_GOOGLE_MAPS_API_KEY=

# Write token — needed for public submission API route and seed script
# Create at sanity.io/manage → API → Tokens → Add API token (Editor role)
SANITY_API_WRITE_TOKEN=
